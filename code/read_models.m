function models = read_models(exp,us,runtype)

% function read_models(s,model)
% 
% reads in the different model types to be used
% takes subject number and runtype as argument (currently unused)

models = {'block'};
